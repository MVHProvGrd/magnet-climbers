# Four new rotors — READY

Clock, pinwheel, dial thermometer, fidget spinner. Four built-in generations; alpha QC and dark/light visual review pass. ready/*.webp exports1024x1024, spindle normalized to(512,512) using visually estimated native centers in pivots.json. Native cutouts retained in keyed, original images in sources.

export.py scales around spindle with474px maximum opaque radius, preserving clearance through rotation. Native image center was NOT the spindle for every object, especially spinner; use exported center or recorded native points, not native bbox center.

These are whole-object rotor images, not separate clock hands or thermometer needles. Claude wires rotation and validates pivot visually. No game code or public/art changes.

31 successful generations this continuation so far. Remaining variety:7 clips,7 bumpers,8 businesses,4S+4N souvenirs.
