{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/quiet-gates.sh\"",
            "timeout": 180,
            "statusMessage": "Running typecheck + tests (silent when green)"
          }
        ]
      }
    ]
  }
}
